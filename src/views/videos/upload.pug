extends ../base

block content
    div.upload__container
        div.upload__video
            video#preview
            button#actionBtn.social__btn Start Recording
        if errorMessage
            span=errorMessage
        form(method="POST", enctype="multipart/form-data")
        label(for="video") Video File
        input(type="file", accept="video/*", required, id="video", name="video")
        label(for="thumbnail") Thumbnail File
        input(type="file", accept="image/*", required, id="thumbnail", name="thumbnail")
        input(name="title", placeholder="Title", required, type="text", maxlength=80)
        input(name="description", placeholder="Description", required, type="text", minlength=5)
        input(name="hashtags", placeholder="#Hashtags, seperated by comma.", required, type="text")
        input(type="submit", value="Upload Video")
        
block scripts
    script(src="/static/js/recorder.js")